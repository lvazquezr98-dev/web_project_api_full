# Dirección del back-end al que llama el front-end.
# Copia este archivo como .env.production antes de ejecutar npm run build
# para el servidor, y pon ahí el subdominio de tu API.
VITE_API_URL=https://api.tu-dominio.com
